spring:
  datasource:
    url: jdbc:sqlite:${PORTFOLIO_DB_PATH:portfolio.db}
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always

eodhd:
  api-key: ${EODHD_API_KEY:}
  base-url: https://eodhd.com/api

server:
  port: 8080

springdoc:
  swagger-ui:
    tryItOutEnabled: true
